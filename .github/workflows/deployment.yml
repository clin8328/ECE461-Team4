name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Create SSH directory
      run: mkdir -p ~/.ssh
      shell: bash
 
    - name: Set up SSH Key
      run: echo "$EC2_SSH_KEY" > ~/.ssh/id_rsa
      env:
        EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      shell: bash 

    - name: Set permissions for SSH key
      run: chmod 600 ~/.ssh/id_rsa
      shell: bash

    - name: SSH into EC2
      run: |
        ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ec2-user@18.227.26.233 "cd ./ECE461_Phase2 && git pull"

# Add your commands for deploying or running tasks on the EC2 instance here
    - name: Retrieve Secret from AWS Secrets Manager on EC2
      run: |
        aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-2:777501491721:secret:EN_content-UcMszU > ./ECE461_Phase2/.env
        
