name: Print Secret

on:
  push:
    branches:
      - main

jobs:
  print-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Print Secret
        run: |
          echo "My secret is: ${{ secrets.EC2_SSH_KEY }}"
